#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

export MACA_EXTENDED_OBSERVATION="${MACA_EXTENDED_OBSERVATION:-False}"
export MACA_DECOUPLED_ACTION_HEADS="${MACA_DECOUPLED_ACTION_HEADS:-True}"
export MACA_ADAPTIVE_SUPPORT_POLICY="${MACA_ADAPTIVE_SUPPORT_POLICY:-True}"
export MACA_SUPPORT_SEARCH_HOLD="${MACA_SUPPORT_SEARCH_HOLD:-6}"
export MACA_RADAR_TRACKING_OBSERVATION="${MACA_RADAR_TRACKING_OBSERVATION:-True}"
export MACA_TRACK_MEMORY_STEPS="${MACA_TRACK_MEMORY_STEPS:-12}"
export MACA_SEMANTIC_SCREEN_OBSERVATION="${MACA_SEMANTIC_SCREEN_OBSERVATION:-True}"
export MACA_SCREEN_TRACK_MEMORY_STEPS="${MACA_SCREEN_TRACK_MEMORY_STEPS:-12}"
export MACA_RELATIVE_MOTION_OBSERVATION="${MACA_RELATIVE_MOTION_OBSERVATION:-True}"
export MACA_DELTA_COURSE_ACTION="${MACA_DELTA_COURSE_ACTION:-True}"
export MACA_COURSE_DELTA_DEG="${MACA_COURSE_DELTA_DEG:-45}"
export MACA_TACTICAL_MODE_OBSERVATION="${MACA_TACTICAL_MODE_OBSERVATION:-False}"
export MACA_COURSE_PRIOR_OBSERVATION="${MACA_COURSE_PRIOR_OBSERVATION:-False}"
export MACA_COURSE_PRIOR_STRENGTH="${MACA_COURSE_PRIOR_STRENGTH:-0.0}"
export MACA_COURSE_HOLD_STEPS="${MACA_COURSE_HOLD_STEPS:-2}"
export MACA_MAX_COURSE_CHANGE_BINS="${MACA_MAX_COURSE_CHANGE_BINS:-2}"
export MACA_LOCK_STATE_OBSERVATION="${MACA_LOCK_STATE_OBSERVATION:-True}"
export MACA_TEAM_STATUS_OBSERVATION="${MACA_TEAM_STATUS_OBSERVATION:-True}"
export MACA_THREAT_STATE_OBSERVATION="${MACA_THREAT_STATE_OBSERVATION:-True}"
export MACA_INTERCEPT_COURSE_ASSIST="${MACA_INTERCEPT_COURSE_ASSIST:-False}"
export MACA_INTERCEPT_COURSE_BLEND="${MACA_INTERCEPT_COURSE_BLEND:-0.0}"
export MACA_INTERCEPT_BREAK_HOLD_BINS="${MACA_INTERCEPT_BREAK_HOLD_BINS:-1}"
export MACA_INTERCEPT_LEAD_DEG="${MACA_INTERCEPT_LEAD_DEG:-25}"
export MACA_COMMIT_DISTANCE="${MACA_COMMIT_DISTANCE:-150}"
export MACA_ATTACK_PRIOR_STRENGTH="${MACA_ATTACK_PRIOR_STRENGTH:-0.0}"

exec bash scripts/run_sf_maca_decoupled_8h_curriculum.sh "$@"

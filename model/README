Local model artifacts live under `model/`.

Expected usage:

- `model/backup/`: ad-hoc local backups only

These directories are intentionally ignored by git. Only lightweight notes such
as this README should be tracked.
